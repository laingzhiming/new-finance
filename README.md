# Neo-Finance TS Pro

## 🚀 酷炫风多端费用统计系统

基于 uni-app + Vue 3 + TypeScript 打造的现代化多端费用管理系统，支持 H5、小程序、App 等多平台。

## ✨ 特性

- 🎨 **玻璃拟态设计** - Glassmorphism 视觉效果
- 🌈 **多主题切换** - Dark、Cyberpunk、Light 三种主题
- 📊 **数据可视化** - 基于 ECharts 的图表展示
- 💎 **TypeScript 严格模式** - 完整的类型定义
- 🎭 **酷炫动效** - 数字滚动、发光效果等
- 📱 **多端适配** - 一套代码多端运行

## 🛠️ 技术栈

- **框架**: uni-app + Vue 3
- **语言**: TypeScript (严格模式)
- **样式**: UnoCSS (Tailwind CSS 兼容)
- **状态管理**: Pinia
- **可视化**: ECharts
- **构建工具**: Vite

## 📦 安装依赖

**重要提示:** 由于 uni-app 的特殊性，建议使用 HBuilderX 或遵循以下步骤：

### 方法一：使用 HBuilderX (推荐)

1. 下载并安装 [HBuilderX](https://www.dcloud.io/hbuilderx.html)
2. 打开 HBuilderX，选择 `文件` -> `导入` -> `从本地目录导入`
3. 选择本项目目录
4. 在 HBuilderX 中运行项目

### 方法二：命令行方式

```bash
# 安装依赖（推荐使用 legacy-peer-deps）
npm install --legacy-peer-deps

# 如果遇到依赖安装问题，可以尝试使用淘宝镜像
npm config set registry https://registry.npmmirror.com
npm install --legacy-peer-deps
```

**注意:**

- 由于 uni-app 依赖版本问题，需要使用 `--legacy-peer-deps` 标志
- 首次运行可能需要较长时间来安装 uni-app 相关依赖。

## 🚀 运行项目

### H5 开发

```bash
npm run dev:h5
```

### 微信小程序开发

```bash
npm run dev:mp-weixin
```

### App 开发

```bash
npm run dev:app
```

## 🏗️ 构建项目

### H5 构建

```bash
npm run build:h5
```

### 微信小程序构建

```bash
npm run build:mp-weixin
```

## 📁 项目结构

```
neo-finance/
├── src/
│   ├── pages/          # 页面
│   │   ├── index/      # 首页 - 3D质感仪表盘
│   │   ├── record/     # 记账页面
│   │   ├── statistics/ # 统计页面
│   │   └── settings/   # 设置页面
│   ├── components/     # 组件
│   ├── types/          # TypeScript 类型定义
│   ├── composables/    # Vue 组合式函数
│   ├── utils/          # 工具函数
│   ├── stores/         # Pinia 状态管理
│   ├── styles/         # 样式文件
│   │   ├── theme.css   # Design Tokens
│   │   └── variables.scss
│   ├── App.vue         # 应用入口
│   ├── main.ts         # 主入口
│   ├── manifest.json   # uni-app 配置
│   └── pages.json      # 页面路由配置
├── vite.config.ts      # Vite 配置
├── tsconfig.json       # TypeScript 配置
├── uno.config.ts       # UnoCSS 配置
└── package.json        # 项目配置
```

## 🎨 主题系统

系统内置三种主题：

- **Dark** - 深色主题
- **Cyberpunk** - 赛博朋克风格
- **Light** - 浅色主题

通过修改根节点的 `data-theme` 属性切换主题。

## 🐳 部署

使用 Nginx + Docker 部署：

```bash
# 构建 H5 版本
npm run build:h5

# Docker 部署 (需要自行配置 Dockerfile 和 nginx.conf)
```

## 📄 License

MIT

---

**Made with ❤️ by Neo-Finance Team**
